<!DOCTYPE html>
<html>

<%- include('../partials/head') %>
<link rel="stylesheet" href="css/index.css">

<body id="index-page">
<div class="container">
    <div class="paper">
        <h1 class="row flex-center align-middle">
            <img class="shadow-large icon-nest" src="images/nest_identity_image.jpg" alt="">
            <p class="padding-left-small">Nestjs Identity server</p>
        </h1>

        <form action=""
              class="login-form row flex-center"
              method="post">
            <div class="sm-12 md-10">
                <h2 class="text-center">Login</h2>

                <div class="form-group row margin-bottom-large">
                    <input type="email"
                           placeholder="User email"
                           class="sm-12"
                           v-model="userEmail">
                </div>

                <div class="form-group row margin-bottom-large">
                    <input type="password"
                           placeholder="Password"
                           class="sm-12"
                           v-model="password">
                </div>

                <fieldset class="form-group">
                    <label for="paperChecks1" class="paper-check">
                        <input type="checkbox"
                               name="paperChecks"
                               id="paperChecks1"
                               v-model="showPassword">
                        <span>Show password</span>
                    </label>

                </fieldset>

            </div>
            <button v-on:submit="showPost"
                    class="btn-secondary btn-block">
                Login
            </button>
        </form>

    </div>

</div>
</body>
<script src="https://cdn.jsdelivr.net/npm/vue@2.6.14/dist/vue.js"></script>
<script>
  const app = new Vue({
    el: '#index-page',
    created() {
      alert('1234142');
    },
    method: {
      showPost: function(e) {
        e.preventDefault();
        alert(test);
      },
    },
    data() {
      console.log('password', this.password);
      return {
        showPassword: true,
        password: '123412',
      };
    },
  });

</script>
</html>
